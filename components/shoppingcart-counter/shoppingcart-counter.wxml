<!-- components/counter/counter.wxml -->
<view class="btns">
    <button catch:tap="reduce" disabled="{{number === 1 ? true : false}}">-</button>
    <text>{{number}}</text>
    <button catch:tap="plus" disabled="{{number == maxNumber ? true : false}}">+</button>
</view>